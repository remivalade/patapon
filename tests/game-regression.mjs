// Régression logique : vraies mathématiques Three.js, DOM et moteur WebGL simulés.
// Ne remplace pas une validation visuelle ni un essai sur téléphone.
import fs from 'node:fs';
import assert from 'node:assert/strict';
import * as THREE from '../src/vendor/three.module.min.js';
const sourceRoot = new URL('../src/', import.meta.url);
const nodes=new Map();function element(){return{style:{},hidden:true,disabled:false,textContent:'',classList:{toggle(){}},setAttribute(){},appendChild(){},addEventListener(type,fn){this[type]=fn},setPointerCapture(){},getBoundingClientRect(){return{left:0,top:0,width:126,height:126}}};}
globalThis.document={getElementById(id){if(!nodes.has(id))nodes.set(id,element());return nodes.get(id)},addEventListener(){}};globalThis.window=globalThis;globalThis.innerWidth=844;globalThis.innerHeight=390;globalThis.devicePixelRatio=2;globalThis.addEventListener=()=>{};globalThis.removeEventListener=()=>{};globalThis.requestAnimationFrame=()=>{};
let captured;class Renderer{constructor(){this.domElement=element();this.shadowMap={};}setPixelRatio(){}setSize(){}render(scene,camera){captured=scene;scene.updateMatrixWorld();camera.updateMatrixWorld();scene.traverse(o=>{assert.ok(o.position.toArray().every(Number.isFinite));if(o.geometry?.attributes.position)for(const p of o.geometry.attributes.position.array)assert.ok(Number.isFinite(p));});}}
globalThis.PataponThree={...THREE,WebGLRenderer:Renderer};globalThis.checkAssert=assert;
let src=fs.readFileSync(new URL('world.js', sourceRoot),'utf8').replace("import * as T from './vendor/three.module.min.js';",'const T=globalThis.PataponThree;').replaceAll(/'\.\/(\w+)\.js'/g,(_, name) => JSON.stringify(new URL(name + '.js', sourceRoot).href));
src+=`\nimport {ROAD_AXIS} from '${new URL('navigation.js', sourceRoot).href}';\n;globalThis.runChecks=()=>{const assert=globalThis.checkAssert,results=[];beginWalk();expansion.updateAnimals(0,.01,player.position);
// The full road can be driven without a border or obstacle on the carriageway.
navNormal=normalAt(73,32);forward.set(0,0,-1).projectOnPlane(navNormal).normalize();player.position.copy(surface(navNormal));riding=true;bikeHeading.copy(forward);drive.speed=48;const start=navNormal.clone();keys.add('w');const frames=2400,dt=2*Math.PI*RADIUS/48/frames;for(let i=0;i<frames;i++)move(dt);keys.clear();assert.ok(navNormal.distanceTo(start)<.004,'Speeder full road circuit');results.push('full-speed road circuit');riding=false;
// Spherical collision footprints must actually deflect movement.
const obstacle=collisions.objects[0];const tangent=new T.Vector3(1,0,0).projectOnPlane(obstacle.n).normalize();const inside=obstacle.n.clone().addScaledVector(tangent,.2/RADIUS).normalize();const out=collisions.resolve(inside,.62,0);assert.ok(out.distanceTo(obstacle.n)*RADIUS>=obstacle.radius+.6,'Tree collision');const big=collisions.objects.find(c=>c.radius>8);const bounced=collisions.resolve(big.n,.62,0);assert.ok(bounced.distanceTo(big.n)*RADIUS>=big.radius+.55,'Rock collision');results.push('tree and rock collisions');
resetHome();jump();let max=0;for(let i=0;i<150;i++){move(.016);max=Math.max(max,jumpHeight);}assert.ok(max>1.5&&jumpHeight===0,'Jump and landing');results.push('jump and landing');
navNormal=normalAt(-29,-15);player.position.copy(surface(navNormal));move(.016);assert.ok(swimming,'Swimming entry');assert.ok(player.position.distanceTo(CENTER)<RADIUS+1.3,'Waterline');navNormal=normalAt(0,30);move(.016);assert.ok(!swimming,'Swimming exit');results.push('swim and leave lake');
resetHome();navNormal=normalAt(8,-49);player.position.copy(surface(navNormal));liftAction();assert.ok(climb?.onPlatform);for(let i=0;i<700;i++)move(.016);assert.equal(expansion.lift.height,TOWER_HEIGHT);assert.equal(climb.y,TOWER_HEIGHT);liftAction();for(let i=0;i<700;i++)move(.016);assert.equal(expansion.lift.height,0);forward.set(1,0,0).applyQuaternion(towerQ);keys.add('w');for(let i=0;i<120;i++)move(.016);keys.clear();assert.equal(climb,null,'Leave elevator at ground');results.push('lift up, down and disembark');
resetHome();navNormal=expansion.bike.position.clone().sub(CENTER).normalize();player.position.copy(surface(navNormal));bikeAction();assert.ok(riding);bikeAction();assert.ok(!riding);results.push('mount and dismount speeder');
const cow=expansion.animals.find(a=>a.kind==='cow');world.updateMatrixWorld(true);const cowUp=cow.n.clone().negate(),cowFront=new T.Vector3(0,0,1).applyQuaternion(cow.root.quaternion);const cowHead=cow.head.getWorldPosition(new T.Vector3());camera.position.copy(cowHead).addScaledVector(cowFront,8).addScaledVector(cowUp,1);camera.up.copy(cowUp);camera.lookAt(cowHead);camera.updateMatrixWorld(true);renderer.domElement.pointerdown({pointerId:42,clientX:innerWidth/2,clientY:innerHeight/2});renderer.domElement.pointerup({pointerId:42,clientX:innerWidth/2,clientY:innerHeight/2});assert.ok(cow.reaction>clock.elapsedTime);const sheep=expansion.animals.find(a=>a.kind==='sheep');greetAnimal(sheep);assert.ok(sheep.reaction>clock.elapsedTime);results.push('cow and sheep interaction');

resetHome();
// Independent touches, immediate jump, camera dead zone and drag history.
const touch=(id,x,y)=>({pointerId:id,clientX:x,clientY:y,preventDefault(){}});
$('jump').pointerdown(touch(3,750,330));assert.equal(jumpVelocity,8.3);resetHome();
renderer.domElement.pointerdown(touch(10,140,300));renderer.domElement.pointermove(touch(10,140,260));assert.ok(joy.y<-.8);
const oldPitch=pitch;renderer.domElement.pointerdown(touch(11,550,220));renderer.domElement.pointermove(touch(11,552,222));assert.equal(pitch,oldPitch);
renderer.domElement.pointermove(touch(11,550,180));assert.ok(pitch>oldPitch);assert.ok(joy.y<-.8);assert.ok(lookMoved);
renderer.domElement.pointermove(touch(11,550,220));assert.ok(lookMoved,'Returning a drag to start is still a drag');renderer.domElement.pointerup(touch(11,550,220));renderer.domElement.pointerup(touch(10,140,260));assert.equal(joy.y,0);assert.equal(lookId,null);results.push('simultaneous touches, jump on press and camera drag threshold');
// Accelerating toward the lake, crossing both shores, hovering and remounting in deep water.
resetHome();navNormal=normalAt(-29,28);forward.set(0,0,-1).projectOnPlane(navNormal).normalize();bikeHeading.copy(forward);riding=true;drive.reset();player.position.copy(surface(navNormal));keys.add('w');let wet=0;
for(let i=0;i<200;i++){move(.016);if(lakeRadius(navNormal)<.7){wet++;assert.ok(Math.abs(expansion.bike.position.distanceTo(CENTER)-(RADIUS-1.24))<.06,'Speeder remains above water');assert.ok(!swimming);}}
keys.clear();assert.ok(wet>40,'Cross lake interior');assert.ok(chart(navNormal).z<-47,'Reach opposite shore');assert.ok(lakeRadius(navNormal)>1);results.push('accelerate and cross lake shore to shore');
navNormal=normalAt(-29,-15);bikeHeading.projectOnPlane(navNormal).normalize();move(.016);assert.ok(lakeWater.uniforms.wake.value>0);bikeAction();move(.016);assert.ok(swimming);assert.equal(getContext().type,'bike');bikeAction();assert.ok(riding);assert.ok(!swimming);results.push('dismount into lake, remount and water wake');
$('brake').pointerdown(touch(12,750,330));for(let i=0;i<40;i++)move(.016);assert.equal(drive.speed,0);$('brake').pointerup(touch(12,750,330));assert.ok(!brakeHeld);results.push('hold brake stops speeder');
// Manual camera orientation does not determine the vehicle course.
resetHome();navNormal=normalAt(73,32);forward.set(0,0,-1).projectOnPlane(navNormal).normalize();bikeHeading.copy(forward);riding=true;drive.speed=20;lastLookTime=clock.elapsedTime;turnView(.5);const lookAngle=forward.angleTo(bikeHeading);keys.add('w');move(.016);assert.ok(Math.abs(forward.angleTo(bikeHeading)-lookAngle)<.001);lastLookTime=-100;move(.016);assert.ok(forward.angleTo(bikeHeading)<lookAngle);keys.add('d');const before=bikeHeading.clone();move(.1);assert.ok(before.angleTo(bikeHeading)>.05);keys.clear();results.push('steering, free look and delayed camera recenter');
// High speed movement cannot pass through a tree trunk.
const trunk=collisions.objects.find(c=>c.height>4&&c.radius<3);const tangent2=new T.Vector3(1,0,0).projectOnPlane(trunk.n).normalize();navNormal=trunk.n.clone().addScaledVector(tangent2,(trunk.radius+6)/RADIUS).normalize();forward.copy(trunk.n).addScaledVector(navNormal,-trunk.n.dot(navNormal)).normalize();bikeHeading.copy(forward);drive.speed=48;player.position.copy(surface(navNormal));keys.add('w');for(let i=0;i<12;i++){move(.045);assert.ok(navNormal.distanceTo(trunk.n)*RADIUS>trunk.radius+1.25);}keys.clear();results.push('speeder collision at full speed');
camera.position.copy(CENTER).add(new T.Vector3(0,-8,0));updateSunEffects(1);assert.ok(innerSun.visible&&sunCage.visible&&sunInteriorUniforms.strength.value>.99);camera.position.set(0,4,0);updateSunEffects(1);assert.ok(!innerSun.visible&&!sunCage.visible);results.push('sun interior halo and spherical cage visibility');

resetHome();navNormal=normalAt(73,32);forward.set(0,0,-1).projectOnPlane(navNormal).normalize();bikeHeading.copy(forward);riding=true;player.position.copy(surface(navNormal));
$('accelerate').pointerdown(touch(30,750,250));assert.ok(accelerateHeld);for(let i=0;i<40;i++)move(.016);assert.ok(drive.speed>18&&drive.speed<21);assert.ok(!$('accelerate').hidden&&!$('brake').hidden&&$('jump').hidden);
$('accelerate').pointerdown(touch(31,750,250));$('accelerate').pointerup(touch(30,750,250));assert.ok(accelerateHeld,'Second finger still holds pedal');
$('brake').pointerdown(touch(32,750,330));for(let i=0;i<30;i++)move(.016);assert.equal(drive.speed,0,'Brake wins over accelerator');$('brake').pointercancel(touch(32,750,330));move(.016);assert.ok(drive.speed>0);$('accelerate').lostpointercapture(touch(31,750,250));assert.ok(!accelerateHeld);for(let i=0;i<30;i++)move(.016);assert.equal(drive.speed,0);results.push('accelerator hold, multiple fingers, brake priority and pointer cancellation');
tilt.state='waiting';tilt.read({beta:55,gamma:0});tilt.read({beta:55,gamma:0});updateHud();assert.ok(tilt.enabled&&$('stick').hidden&&!$('recenter').hidden);tilt.read({beta:55,gamma:30});
$('accelerate').pointerdown(touch(34,750,250));const headingStart=bikeHeading.clone();for(let i=0;i<35;i++)move(.016);assert.ok(drive.speed>15,'Accelerator works in tilt mode');assert.ok(headingStart.angleTo(bikeHeading)>.2,'Sensor changes course');assert.ok(forward.angleTo(bikeHeading)<.6,'Camera follows turn');
renderer.domElement.pointerdown(touch(35,550,200));renderer.domElement.pointerup(touch(35,550,200));assert.equal(lookId,null,'Camera pointer released in tilt mode');
$('recenter').click();assert.ok(!accelerateHeld);tilt.read({beta:55,gamma:30});assert.equal(tilt.update(.016),0,'Recenter while holding a comfortable pose');
$('accelerate').pointerdown(touch(36,750,250));orientationChanged();assert.ok(!accelerateHeld);assert.equal(drive.speed,0,'Screen rotation releases throttle');tilt.stop();updateHud();assert.ok(!$('stick').hidden);results.push('tilt driving, automatic camera, recenter and rotate phone');
$('accelerate').pointerdown(touch(37,750,250));bikeAction();assert.ok(!riding&&!accelerateHeld);move(.016);assert.ok($('accelerate').hidden&&$('brake').hidden&&!$('jump').hidden);results.push('dismount clears pedals and restores walking controls');

// Both kinds are actual moving mounts, including when their grazing update runs.
for(const kind of ['cow','sheep']){resetHome();const a=expansion.animals.find(a=>a.kind===kind);a.n=normalAt(73,32);a.root.position.copy(surface(a.n));a.root.quaternion.copy(orientation(a.n));navNormal=a.n.clone();player.position.copy(surface(navNormal));contextAction();assert.equal(mountedAnimal,a);assert.ok(a.ridden&&riding);updateHud();assert.ok(!$('jump').hidden);const initial=a.n.clone();keys.add('w');jump();let highest=0;for(let i=0;i<160;i++){move(.016);expansion.updateAnimals(i*.016,.016,player.position);highest=Math.max(highest,jumpHeight);assert.ok(a.n.distanceTo(navNormal)<1e-10);assert.ok(a.root.position.distanceTo(surface(navNormal,animalGround(navNormal)+jumpHeight))<1e-8);}
keys.clear();assert.ok(highest>2.7&&jumpHeight===0);assert.ok(initial.distanceTo(navNormal)*RADIUS>15);tilt.state='waiting';tilt.read({beta:55,gamma:0});tilt.read({beta:55,gamma:0});tilt.read({beta:55,gamma:25});$('accelerate').pointerdown(touch(65,750,250));const oldHeading=bikeHeading.clone();for(let i=0;i<20;i++)move(.016);assert.ok(oldHeading.angleTo(bikeHeading)>.05);tilt.stop();const parked=a.n.clone();bikeAction();assert.ok(!riding&&!mountedAnimal&&!a.ridden);expansion.updateAnimals(80,.016,player.position);assert.ok(a.n.distanceTo(parked)<1e-9,'Released animal stays where ridden');}
results.push('cow and sheep mount, gallop, tilt, jump, land and remain after dismount');
// New lake water, exposed island, and dry bridge use the same terrain functions.
resetHome();const bridge=landscape.bridgeSamples.find(n=>bigLakeRadius(n)<.6);assert.ok(bridge);navNormal=bridge.clone();player.position.copy(surface(navNormal,roadOffset(navNormal)));forward.copy(new T.Vector3().crossVectors(ROAD_AXIS,navNormal)).normalize();keys.add('w');for(let i=0;i<70;i++){move(.016);assert.ok(!swimming);assert.ok(player.position.distanceTo(CENTER)<RADIUS-2);}keys.clear();results.push('walk across the elevated bridge without swimming');
navNormal=landscape.island.clone();clearInput();move(.016);assert.ok(!swimming);assert.ok(player.position.distanceTo(CENTER)<RADIUS-1);assert.ok(islandDistance(navNormal)<1);assert.ok(collisions.objects.filter(c=>islandDistance(c.n)<17).length>=6);results.push('raised island with six collidable trees');
navNormal=landscape.island.clone().addScaledVector(ROAD_AXIS,-.13).normalize();clearInput();move(.016);assert.ok(bigLakeRadius(navNormal)<1&&swimming,'Swim in new lake');const farWaterPoint=navNormal.clone();expansion.bike.position.copy(surface(navNormal,1+hoverBase(navNormal)));player.position.copy(surface(navNormal,lakeDepth(navNormal)-1));bikeAction();move(.016);assert.ok(riding&&!swimming);assert.ok(Math.abs(expansion.bike.position.distanceTo(CENTER)-(RADIUS-1.24))<.1);bikeAction();move(.016);assert.ok(swimming);results.push('swim and use speeder on the large lake');
const homeN=normalAt(29,-4),homePosition=surface(homeN);dayNight.update(0,homePosition);assert.ok(daylightAt(homeN,dayNight.direction.value)>.99);const lit=scene.fog.color.clone();dayNight.update(90,homePosition);assert.ok(daylightAt(homeN,dayNight.direction.value)<.01);assert.ok(daylightAt(homeN.clone().negate(),dayNight.direction.value)>.99);assert.ok(scene.fog.color.r<lit.r*.2);const capDirection=new T.Vector3(0,1,0).applyQuaternion(dayNight.cap.quaternion);assert.ok(capDirection.distanceTo(dayNight.direction.value)<1e-9);assert.ok(distantWater.uniforms.shadeDirection.value.distanceTo(capDirection)<1e-9);dayNight.update(180,homePosition);assert.ok(daylightAt(homeN,dayNight.direction.value)>.99);
for(const material of dayNight.patched){const shader={uniforms:{},vertexShader:T.ShaderLib.standard.vertexShader,fragmentShader:T.ShaderLib.standard.fragmentShader};material.onBeforeCompile(shader);assert.ok(shader.vertexShader.includes('solarWorldPosition=(modelMatrix*solarPosition).xyz'));assert.ok(shader.fragmentShader.includes('reflectedLight.directDiffuse*=1.-shade*.975'));assert.equal(shader.uniforms.shadeDirection,dayNight.direction);}
results.push('moving hemispherical cover, opposite day/night, water lighting and full three-minute cycle');

assert.ok(forest.groups.every(g=>g.length>0),'All three tree silhouettes');assert.equal(fields.locations.length,2100);for(const n of fields.locations){assert.ok(roadDistance(n)>=7);assert.ok(lakeRadius(n)>=1.08);assert.ok(collisions.nearby(n).every(c=>c.n.distanceTo(n)*RADIUS>c.radius),'Flower clearings remain open');}
for(const material of dayNight.patched){if(!material.userData.wind)continue;const shader={uniforms:{},vertexShader:T.ShaderLib.standard.vertexShader,fragmentShader:T.ShaderLib.standard.fragmentShader};material.onBeforeCompile(shader);assert.ok(shader.vertexShader.includes('uniform float windTime'));assert.ok(shader.vertexShader.includes('solarWorldPosition=(modelMatrix*solarPosition).xyz'));assert.ok(shader.uniforms.windTime);}
results.push('three tree silhouettes, clear flower fields and composed wind/night shaders');
const meadow=expansion.meadowNormal(0,0);fields.update(1,surface(meadow),meadow.clone().negate());assert.ok(fields.wings.visible);for(const batch of [fields.wings,fields.bodies])assert.ok(batch.instanceMatrix.array.every(Number.isFinite));fields.update(2,surface(meadow),meadow);assert.ok(!fields.wings.visible,'Butterflies rest at night');
nightDetails.update(1,surface(meadow),meadow);assert.ok(nightDetails.flies.uniforms.shadeDirection.value.distanceTo(meadow)<1e-9);assert.equal(nightDetails.fireflies.length,150);assert.ok(nightDetails.lampPositions.length>6);results.push('local daytime butterflies and night light uniforms');
const state={normal:normalAt(0,25),position:surfacePoint(0,25),forward:new T.Vector3(0,0,-1),riding:true,animal:{kind:'cow'},jumpHeight:0,climb:null,mode:'walk'};let effectTime=0;const effectStep=dt=>{effectTime+=dt;return trailEffects.update(effectTime,dt,state,dayNight.direction.value,lakeWater.uniforms.mist.value);};
trailEffects.reset();effectStep(.016);const dustBefore=trailEffects.stats.dust;for(let i=0;i<15;i++){state.normal=advanceFrame(state.normal,state.forward,0,1,.2).normal;state.position=surface(state.normal);effectStep(.016);}assert.ok(trailEffects.stats.dust>dustBefore,'Hoof dust');state.jumpHeight=2;effectStep(.016);const landBefore=trailEffects.stats.landings;state.jumpHeight=0;effectStep(.016);assert.equal(trailEffects.stats.landings,landBefore+1);
state.normal=meadow.clone();state.position=surface(state.normal);state.animal=null;trailEffects.reset();effectStep(.016);const grassBefore=trailEffects.stats.grass;for(let i=0;i<15;i++){state.normal=advanceFrame(state.normal,state.forward.clone().projectOnPlane(state.normal).normalize(),0,1,.2).normal;state.position=surface(state.normal);effectStep(.016);}assert.ok(trailEffects.stats.grass>grassBefore,'Speeder stirs grass');
state.riding=false;state.normal=normalAt(-29,20);state.position=surface(state.normal);trailEffects.reset();effectStep(.016);const entryBefore=trailEffects.stats.waterEntries;state.normal=normalAt(-29,10);state.position=surface(state.normal);effectStep(.016);assert.equal(trailEffects.stats.waterEntries,entryBefore+1);assert.ok(trailEffects.stats.splash>=22);results.push('hoof dust, landing clouds, speeder grass and water-entry splashes');
const goldTree=forest.groups[2][0];state.normal=goldTree.n.clone();state.position=surface(state.normal);trailEffects.reset();const leavesBefore=trailEffects.stats.leaves;for(let i=0;i<15;i++)effectStep(.05);assert.ok(trailEffects.stats.leaves>leavesBefore,'Leaves fall near leafy trees');
trailEffects.emit(state.normal,state.position,0,700);state.climb={};let alive=0;for(let i=0;i<170;i++){alive=effectStep(.05);assert.ok(alive<=320);for(const attr of Object.values(trailEffects.points.geometry.attributes))assert.ok(attr.array.every(Number.isFinite));}assert.equal(alive,0);assert.equal(trailEffects.particles.length,320);results.push('nearby falling leaves and bounded particle pool expires cleanly');
return results;};`;
try{await import('data:text/javascript;base64,'+Buffer.from(src).toString('base64'));}catch(e){console.error(e.message);process.exit(1)}
let count=0,triangles=0;captured.traverse(o=>{if(o.geometry){count++;if(!o.isPoints)triangles+=(o.geometry.index?.count||o.geometry.attributes.position.count)/3*(o.count||1);}});console.log({meshes:count,triangles});try{console.log(globalThis.runChecks());}catch(e){console.error(e.message);process.exitCode=1;}
